export type Theme = {
    valid: string,
    invalid: string,
    misplaced: string,
    bgValid: string,
    bgInvalid: string,
    bgMisplaced: string,
    borderValid: string,
    textValid: string,
}

export function getThemedClassName(baseClassName: string, theme: Theme) {
    return ``;
}